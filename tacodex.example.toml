# Tacodex 設定ファイル
#
# このファイルをプロジェクトルートに `tacodex.toml` としてコピーしてください。

# 子エージェント用のモデル（省略時は親と同じ）
# child_agent_model = "gpt-5.2-codex"

# エージェントごとのタイムアウト（秒）
agent_timeout_sec = 300

# 最大並列エージェント数
max_parallel_agents = 4
